# GraphQL schema snapshot

The file **`graphql-schema.snapshot.graphql`** was copied from a development environment for **orientation only**. It is **not** guaranteed to match your Unraid version.

- Prefer Unraid’s official API / release notes when upgrading.
- Regenerate locally if needed: export schema from your server’s GraphQL introspection and replace the snapshot (keep sensitive operations out of public forks if you extend it).
